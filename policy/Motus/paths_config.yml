# Motus Evaluation Path Configuration
# Paths needed for inference

# ============================================================================
# Core Paths - MUST MODIFY THESE
# ============================================================================
robotwin_root: "/scratch/ows/argus/extern/RoboTwin"
conda_env: "/scratch/ows/argus/.venv/motus"
checkpoint_path: "motus-robotics/Motus_robotwin2"

# Pretrained model paths (only for loading configs, not weights)
wan_path: "/scratch/ows/argus/extern/X-WAM/checkpoints/wan22_5b"
vlm_path: "Qwen/Qwen3-VL-2B-Instruct"

# ============================================================================
# Optional Configuration
# ============================================================================
# GPU IDs (empty means auto-detect)
gpu_ids: []  # e.g., [0, 1, 2, 3, 4, 5, 6, 7]

# Task configuration
task_config: "demo_randomized"
seed: 42
tasks_file: "tasks_all.txt"
